import { PmsAcceptanceStatus } from './pms-acceptance-status.model';

describe('PmsAcceptanceStatus', () => {
  it('should create an instance', () => {
    expect(new PmsAcceptanceStatus()).toBeTruthy();
  });
});
