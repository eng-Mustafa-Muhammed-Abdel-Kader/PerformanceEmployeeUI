import { PmsEmployeePlanObjective } from './pms-employee-plan-objective.model';

describe('PmsEmployeePlanObjective', () => {
  it('should create an instance', () => {
    expect(new PmsEmployeePlanObjective()).toBeTruthy();
  });
});
